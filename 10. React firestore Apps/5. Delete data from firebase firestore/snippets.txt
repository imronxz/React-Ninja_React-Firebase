_RecipeList.js

// firebase/firestore
import { projectFirestore } from '../firebase/config';
// icon
import deleteIcon from '../assets/delete-icon.svg';
  // TODO: handleClick delete -> doc(id) -> collection('recipes')
  const handleClick = (id) => {
    projectFirestore.collection('recipes').doc(id).delete();
  };

        return(
          <img
            src={deleteIcon}
            alt="delete-icon"
            className="delete"
            onClick={() => handleClick(recipe.id)} //FIXME: handleClick taking (recipe.id)
          />
        )

_RecipeList.css
/* delete db */
.delete {
  position: absolute;
  top: 10px;
  right: 10px;
  /* background: #b70233; */
  color: #fff;
  font-size: 1.2em;
  padding: 5px 10px;
  border-radius: 4px;
  cursor: pointer;
  filter: invert(50%);
}